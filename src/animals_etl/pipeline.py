from __future__ import annotations

async def run_etl(*, base_url: str, concurrency: int, batch_size: int, retries: int,
                  connect_timeout: float, read_timeout: float) -> None:
    # Stub for initial project structure commit.
    # Real ETL will be added in the next commit.
    print("ETL pipeline skeleton ready. Implementation comes in the next commit.")